name: awe-platform

networks:
  awe-network:
    driver: bridge

volumes:
  rabbitmq-data:
  postgres-data:
  minio-data:
  redis-data:

services:

  #MINI IO
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    ports:
      - "${MINIO_PORT}:9000"
      - "${MINIO_MANAGEMENT_PORT}:9001"
    volumes:
      - minio-data:/data 
  # DATABASE POSTGRESQL
  postgres:
      image: postgres:${POSTGRES_IMAGE_TAG}
      container_name: awe-postgres
      restart: always
      ports:
        - "${POSTGRES_PORT}:5432"
      environment:
        POSTGRES_USER: ${POSTGRES_USER}
        POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        # POSTGRES_DB: awe_db
      volumes:
        - postgres-data:/var/lib/postgresql/data
        - ./init-dbs.sh:/docker-entrypoint-initdb.d/init-dbs.sh:ro
      networks:
        - awe-network
      healthcheck:
        test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d awe_db"]
        interval: 5s
        timeout: 5s
        retries: 10

  # ----------------------------------------
  # 3. KEYCLOAK (Identity & SSO)
  # ----------------------------------------
  keycloak:
    image: quay.io/keycloak/keycloak:latest
    container_name: awe-keycloak
    command: start-dev --import-realm
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak_db
      KC_DB_USERNAME: ${POSTGRES_USER}
      KC_DB_PASSWORD: ${POSTGRES_PASSWORD}
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN_USER:-admin}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
      KC_HOSTNAME_STRICT: "false"
      KC_HTTP_ENABLED: "true"
      OIDC_CLIENT_SECRET: ${OIDC_CLIENT_SECRET}
    ports:
      - "${KEYCLOAK_PORT:-8081}:8080"
    volumes:
      - ./keycloak-init:/opt/keycloak/data/import:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - awe-network

  # REDIS
  redis:
    image: redis:${REDIS_IMAGE_TAG}
    container_name: awe-redis
    restart: always
    ports:
      - "${REDIS_PORT:-6379}:6379"
    command: redis-server --requirepass "${REDIS_PASSWORD}" --appendonly yes
    volumes:
      - redis-data:/data
    networks:
      - awe-network
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # REDIS INSIGHT (GUI)
  redisinsight:
    image: redis/redisinsight:latest
    container_name: awe-redisinsight
    restart: unless-stopped
    ports:
      - "${REDISINSIGHT_PORT:-5540}:5540"
    networks:
      - awe-network
    depends_on:
      redis:
        condition: service_healthy

  # RABBITMQ   
  rabbitmq:
    image: rabbitmq:${RABBITMQ_IMAGE_TAG}
    container_name: awe-rabbitmq
    hostname: rabbitmq
    restart: unless-stopped

    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}
      RABBITMQ_DEFAULT_VHOST: /
 
    ports:
      - "${RABBITMQ_AMQP_PORT}:5672"
      - "${RABBITMQ_MANAGEMENT_PORT}:15672"

    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
      - ./rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf:ro

    networks:
      - awe-network

    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
  # SETUP WORKER 
  rabbitmq-setup:
    image: curlimages/curl
    container_name: awe-rabbitmq-setup
    depends_on:
      rabbitmq:
        condition: service_healthy
    networks:
      - awe-network
    environment:
      RABBITMQ_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_PASS: ${RABBITMQ_DEFAULT_PASS}
      RABBITMQ_VHOST: ${RABBITMQ_DEFAULT_VHOST}
      RABBITMQ_HOST: rabbitmq
    command: |
      /bin/sh -c "
      echo 'Waiting for RabbitMQ Management API...'
      
      # Retry loop
      while ! curl -s -f -u $${RABBITMQ_USER}:$${RABBITMQ_PASS} http://$${RABBITMQ_HOST}:15672/api/aliveness-test/%2F; do
        echo 'RabbitMQ not ready... retrying in 3s'
        sleep 3
      done
      
      echo 'RabbitMQ is UP! Configuring...'

      # create VHost 
      echo 'Creating VHost: $${RABBITMQ_VHOST}'
      curl -i -u $${RABBITMQ_USER}:$${RABBITMQ_PASS} -H 'content-type:application/json' -X PUT http://$${RABBITMQ_HOST}:15672/api/vhosts/$${RABBITMQ_VHOST}

      # Set Permissions 
      echo 'Setting Permissions for user: $${RABBITMQ_USER}'
      curl -i -u $${RABBITMQ_USER}:$${RABBITMQ_PASS} -H 'content-type:application/json' -X PUT http://$${RABBITMQ_HOST}:15672/api/permissions/$${RABBITMQ_VHOST}/$${RABBITMQ_USER} -d '{\"configure\":\".*\",\"write\":\".*\",\"read\":\".*\"}'
      
      echo 'SETUP COMPLETED SUCCESSFULLY!'
      "

  # ASPIRE DASHBOARD
  aspire-dashboard:
    image: mcr.microsoft.com/dotnet/aspire-dashboard:13
    container_name: awe-aspire-dashboard
    restart: unless-stopped
    ports:
      - "${DASHBOARD_UI_PORT:-19888}:18888"
      - "${DASHBOARD_OTLP_PORT:-19889}:18889"
      - "${DASHBOARD_OTLP_HTTP_PORT:-19890}:18890"
    environment:
      - DOTNET_DASHBOARD_UNSECURED_ALLOW_ANONYMOUS=true
      # --- 3. Cấu hình OTLP ---
      - Dashboard__Otlp__AuthMode=ApiKey
      - Dashboard__Otlp__PrimaryApiKey=${DASHBOARD_OTLP_TOKEN}
    networks:
      - awe-network
    depends_on:
      - keycloak

  # WORKER ENGINE
  # Pulls pre-built image from GHCR — no local source required for self-host
  worker-engine:
    image: ${IMAGE_REGISTRY}/awe-engine-worker:${IMAGE_TAG:-latest}
    container_name: awe-engine-worker
    restart: unless-stopped

    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT}

      # ── RabbitMQ ──────────────────────────────────────────────────────────
      # Key format: Section__Property maps to appsettings: { "RabbitMq": { "Host": ... } }
      RabbitMq__Host: ${RABBITMQ_HOST}
      RabbitMq__Port: ${RABBITMQ_PORT}
      RabbitMq__VirtualHost: ${RABBITMQ_VHOST}
      RabbitMq__Username: ${RABBITMQ_USERNAME}
      RabbitMq__Password: ${RABBITMQ_PASSWORD}
      RabbitMq__PrefetchCount: ${MT_PREFETCH_COUNT:-16}

      # ── Database ──────────────────────────────────────────────────────────
      ConnectionStrings__postgres: Host=awe-postgres;Port=5432;Database=awe_db;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      ConnectionStrings__Redis: awe-redis:6379,password=${REDIS_PASSWORD},abortConnect=false

      # ── MinIO ─────────────────────────────────────────────────────────────
      Minio__Endpoint: ${MINIO_ENDPOINT:-minio:9000}
      Minio__AccessKey: ${MINIO_ROOT_USER}
      Minio__SecretKey: ${MINIO_ROOT_PASSWORD}
      Minio__BucketName: awe-plugins
      Minio__UseSSL: "false"

      # ── Telegram Alerts (optional) ────────────────────────────────────────
      TelegramNotificationConfig__BotToken: ${TELEGRAM_BOT_TOKEN:-}
      TelegramNotificationConfig__ChatID: ${TELEGRAM_CHAT_ID:-}

    depends_on:
      rabbitmq:
        condition: service_healthy

    networks:
      - awe-network

  # API GATEWAY
  api-gateway:
    image: ${IMAGE_REGISTRY}/awe-api-gateway:${IMAGE_TAG:-latest}
    container_name: awe-api-gateway
    restart: unless-stopped

    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT}

      # ── RabbitMQ ──────────────────────────────────────────────────────────
      RabbitMq__Host: ${RABBITMQ_HOST}
      RabbitMq__Port: ${RABBITMQ_PORT}
      RabbitMq__VirtualHost: ${RABBITMQ_VHOST}
      RabbitMq__Username: ${RABBITMQ_USERNAME}
      RabbitMq__Password: ${RABBITMQ_PASSWORD}
      RabbitMq__PrefetchCount: ${MT_PREFETCH_COUNT:-16}

      # ── Database ──────────────────────────────────────────────────────────
      ConnectionStrings__postgres: Host=awe-postgres;Port=5432;Database=awe_db;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      ConnectionStrings__Redis: awe-redis:6379,password=${REDIS_PASSWORD},abortConnect=false

      # ── MinIO ─────────────────────────────────────────────────────────────
      Minio__Endpoint: ${MINIO_ENDPOINT:-awe-minio:9000}
      Minio__AccessKey: ${MINIO_ROOT_USER}
      Minio__SecretKey: ${MINIO_ROOT_PASSWORD}
      Minio__BucketName: awe-plugins
      Minio__UseSSL: "false"

      # ── Keycloak (internal DNS — JWT validation is server-side) ───────────
      Keycloak__Authority: ${KEYCLOAK_INTERNAL_AUTHORITY:-http://awe-keycloak:8080/realms/awe-auth}
      Keycloak__ValidIssuer: ${OIDC_AUTHORITY}
      # ── Telegram Alerts (optional) ────────────────────────────────────────
      TelegramNotificationConfig__BotToken: ${TELEGRAM_BOT_TOKEN:-}
      TelegramNotificationConfig__ChatID: ${TELEGRAM_CHAT_ID:-}

    ports:
      - "${API_GATEWAY_HTTP_PORT}:8080"
      - "${API_GATEWAY_HTTPS_PORT}:8443"

    depends_on:
      rabbitmq:
        condition: service_healthy

    networks:
      - awe-network


  # PLUGIN WORKER
  worker:
    image: ${IMAGE_REGISTRY}/awe-plugin-worker:${IMAGE_TAG:-latest}
    container_name: awe-plugin-worker
    restart: unless-stopped

    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT}

      # ── RabbitMQ ──────────────────────────────────────────────────────────
      RabbitMq__Host: ${RABBITMQ_HOST}
      RabbitMq__Port: ${RABBITMQ_PORT}
      RabbitMq__VirtualHost: ${RABBITMQ_VHOST}
      RabbitMq__Username: ${RABBITMQ_USERNAME}
      RabbitMq__Password: ${RABBITMQ_PASSWORD}
      RabbitMq__PrefetchCount: 1

      # ── Database ──────────────────────────────────────────────────────────
      ConnectionStrings__postgres: Host=awe-postgres;Port=5432;Database=awe_db;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      ConnectionStrings__Redis: awe-redis:6379,password=${REDIS_PASSWORD},abortConnect=false

      # ── MinIO (plugin storage) ──────────────────────────────────────────
      Minio__Endpoint: ${MINIO_ENDPOINT:-awe-minio:9000}
      Minio__AccessKey: ${MINIO_ROOT_USER}
      Minio__SecretKey: ${MINIO_ROOT_PASSWORD}
      Minio__BucketName: awe-plugins
      Minio__UseSSL: "false"

    depends_on:
      rabbitmq:
        condition: service_healthy

    networks:
      - awe-network

  # FRONTEND
  frontend:
    image: ${IMAGE_REGISTRY}/awe-frontend:${IMAGE_TAG:-latest}
    container_name: awe-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-80}:80"
    # Runtime config injection — entrypoint.sh reads these env vars at startup
    # and generates /usr/share/nginx/html/config.js via envsubst.
    # The React bundle is built generically (no VITE_* baked in),
    # so changing these values only requires a container restart — no rebuild.
    environment:
      API_URL: ${FRONTEND_API_URL:-/api}
      SIGNALR_URL: ${FRONTEND_SIGNALR_URL:-/hubs/workflow}
      OIDC_AUTHORITY: ${OIDC_AUTHORITY}
      OIDC_CLIENT_ID: ${OIDC_FE_CLIENT_ID:-awe-fe}
      OIDC_REDIRECT_URI: ${OIDC_REDIRECT_URI:-http://localhost/}
      OIDC_POST_LOGOUT_REDIRECT_URI: ${OIDC_POST_LOGOUT_REDIRECT_URI:-http://localhost/}
      OIDC_SCOPE: ${OIDC_SCOPE:-openid profile email roles}
    depends_on:
      - api-gateway
    networks:
      - awe-network
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
